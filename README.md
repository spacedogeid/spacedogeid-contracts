# This is Space Doge ID contracts project


# Commands

copy `.example.env` to `.env` and edit it 

```
npx hardhat test
npx hardhat --network binance deploy
npx hardhat --network binance verify [address] [arg1] [arg2]...
```
